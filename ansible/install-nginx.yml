---
- name: Install and configure Nginx
  hosts: localhost
  become: yes

  tasks:
    - name: Install Nginx package
      apt:
        name: nginx
        state: present
        update_cache: yes

    - name: Ensure Nginx service is running
      service:
        name: nginx
        state: started
        enabled: yes
